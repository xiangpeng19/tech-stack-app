<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">THETA GANG</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/trades">Trades</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/admin">Admin management</a>
        </li>
      </ul>
      <% if user_signed_in? %>
      Logged in as: <strong><%= current_user.email %></strong>.
      <%= link_to 'Edit profile', edit_user_registration_path, :class => 'btn btn-light' %>
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-light'  %>
      <% else %>
      <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-light'  %>
      <%= link_to "Login", new_user_session_path, :class => 'btn btn-light'  %>
      <% end %>
    </div>
  </div>
</nav>